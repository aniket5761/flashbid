spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flashbid
    username: user
    password: password

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: thisisthesecretkeythisisthesecretkeythisisthesecretkey

app:
  redis:
    pubsub-enabled: true
    
  allowed-origins: http://localhost:5173